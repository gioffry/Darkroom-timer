--- a/build_darkroom.py
+++ b/build_darkroom.py
@@ -33,2 +33,2 @@
-VERSION_NAME = "0.6.1"
-VERSION_CODE = "27"
+VERSION_NAME = "0.6.2"
+VERSION_CODE = "28"
@@ -42 +42 @@
-    print(f"[Darkroom v0.6.1] {msg}", flush=True)
+    print(f"[Darkroom v0.6.2] {msg}", flush=True)
@@ -237,3 +237,3 @@
-        if not re.search(r"versionCode\s+27\b", g) or not re.search(r"versionName\s+['\"]0\.6\.1['\"]", g):
-            fail("app/build.gradle non riporta versionCode 27 / versionName 0.6.1")
-    log("Preflight v0.6.1 OK: manifest/versione/requisiti SONOFF invarianti verificati")
+        if not re.search(r"versionCode\s+28\b", g) or not re.search(r"versionName\s+['\"]0\.6\.2['\"]", g):
+            fail("app/build.gradle non riporta versionCode 28 / versionName 0.6.2")
+    log("Preflight v0.6.2 OK: manifest/versione/requisiti SONOFF invarianti verificati")
--- a/project/app/build.gradle
+++ b/project/app/build.gradle
@@ -13,2 +13,2 @@
-        versionCode 27
-        versionName '0.6.1'
+        versionCode 28
+        versionName '0.6.2'
--- a/project/app/src/main/AndroidManifest.xml
+++ b/project/app/src/main/AndroidManifest.xml
@@ -4,2 +4,2 @@
-    android:versionCode="27"
-    android:versionName="0.6.1">
+    android:versionCode="28"
+    android:versionName="0.6.2">
--- a/project/app/src/main/java/it/darkroom/timer/MainActivity.java
+++ b/project/app/src/main/java/it/darkroom/timer/MainActivity.java
@@ -75,6 +75,6 @@
     private static final int REQ_EXPORT_BACKUP = 4101;
     private static final int REQ_IMPORT_BACKUP = 4102;
     private static final int REQ_EXPORT_JPG = 4103;
-    private static final String APP_VERSION = "0.6.1";
+    private static final String APP_VERSION = "0.6.2";
 
     private static final class FoundDevice {
@@ -420,13 +420,13 @@
 
         LinearLayout modeRow = new LinearLayout(this);
         modeRow.setOrientation(LinearLayout.HORIZONTAL);
-        printModeButton = modeButton("STAMPA");
-        testModeButton = modeButton("PROVINO");
-        logModeButton = modeButton("LOG");
+        printModeButton = modeButton("◉\nSTAMPA");
+        testModeButton = modeButton("▦\nPROVINO");
+        logModeButton = modeButton("≡\nLOG");
         printModeButton.setOnClickListener(v -> setMode(MODE_PRINT));
         testModeButton.setOnClickListener(v -> setMode(MODE_TEST));
         logModeButton.setOnClickListener(v -> setMode(MODE_LOG));
-        modeRow.addView(printModeButton, margin(lp(0, dp(52), 1f), 0, 0, dp(4), 0));
-        modeRow.addView(testModeButton, margin(lp(0, dp(52), 1f), dp(4), 0, dp(4), 0));
-        modeRow.addView(logModeButton, margin(lp(0, dp(52), 0.72f), dp(4), 0, 0, 0));
+        modeRow.addView(printModeButton, margin(lp(0, dp(68), 1f), 0, 0, dp(4), 0));
+        modeRow.addView(testModeButton, margin(lp(0, dp(68), 1f), dp(4), 0, dp(4), 0));
+        modeRow.addView(logModeButton, margin(lp(0, dp(68), 1f), dp(4), 0, 0, 0));
         root.addView(modeRow, margin(lp(-1, -2), 0, 0, 0, 12));
@@ -464,5 +464,5 @@
         saveLogButton.setTextSize(16);
         saveLogButton.setTypeface(Typeface.DEFAULT, Typeface.BOLD);
         saveLogButton.setAllCaps(false);
-        saveLogButton.setTextColor(Color.WHITE);
+        saveLogButton.setTextColor(darkroomMode ? Color.BLACK : Color.WHITE);
         saveLogButton.setBackground(roundRect(darkroomMode ? RED : LOG_ACCENT, 10, 0, 0));
@@ -713,5 +713,5 @@
     private void styleLogFilterButton(Button b, boolean selected) {
         int accent = darkroomMode ? RED : LOG_ACCENT;
         b.setBackground(roundRect(selected ? accent : BUTTON, 8, selected ? 0 : 1, BORDER));
-        b.setTextColor(selected ? Color.WHITE : MUTED);
+        b.setTextColor(selected ? (darkroomMode ? Color.BLACK : Color.WHITE) : MUTED);
     }
@@ -944,10 +944,7 @@
     private void showLogEditor(final LogEntry entry, final boolean isNew) {
         if (darkroomMode) {
-            new AlertDialog.Builder(this)
-                    .setTitle("Compilazione scheda")
-                    .setMessage("Per scrivere nei campi manuali è meglio uscire dalla modalità camera oscura: la tastiera di Android non è inattinica. Puoi comunque consultare il LOG in rosso.")
-                    .setPositiveButton("VAI ALLE IMPOSTAZIONI", (d, w) -> showSettingsDialog())
-                    .setNegativeButton("ANNULLA", null)
-                    .show();
+            showAppConfirmDialog("COMPILAZIONE SCHEDA",
+                    "Per scrivere nei campi manuali è meglio uscire dalla modalità camera oscura: la tastiera di Android non è inattinica. Puoi comunque consultare il LOG in rosso.",
+                    "VAI ALLE IMPOSTAZIONI", this::showSettingsDialog, "ANNULLA");
             return;
         }
@@ -1073,5 +1070,5 @@
 
         Button save = compactButton("SALVA SCHEDA");
-        save.setBackground(roundRect(GREEN, 9, 0, 0));
-        save.setTextColor(Color.BLACK);
+        save.setBackground(roundRect(darkroomMode ? RED : LOG_ACCENT, 9, 0, 0));
+        save.setTextColor(darkroomMode ? Color.BLACK : Color.WHITE);
         save.setOnClickListener(v -> {
@@ -1310,4 +1307,57 @@
         return v.isEmpty() ? "scheda" : v;
     }
 
+    private void showAppConfirmDialog(String title, String message, String positiveLabel,
+                                      Runnable positiveAction, String negativeLabel) {
+        if (!darkroomMode) {
+            AlertDialog.Builder builder = new AlertDialog.Builder(this)
+                    .setTitle(title)
+                    .setMessage(message);
+            if (positiveLabel != null) {
+                builder.setPositiveButton(positiveLabel, (d, w) -> {
+                    if (positiveAction != null) positiveAction.run();
+                });
+            }
+            if (negativeLabel != null) builder.setNegativeButton(negativeLabel, null);
+            builder.show();
+            return;
+        }
+
+        final Dialog dialog = new Dialog(this);
+        dialog.requestWindowFeature(Window.FEATURE_NO_TITLE);
+        LinearLayout panel = new LinearLayout(this);
+        panel.setOrientation(LinearLayout.VERTICAL);
+        panel.setPadding(dp(20), dp(18), dp(20), dp(18));
+        panel.setBackground(roundRect(Color.BLACK, 14, 1, BORDER));
+
+        TextView heading = text(title, 19, RED, true);
+        panel.addView(heading, margin(lp(-1, -2), 0, 0, 0, 12));
+        if (message != null && !message.trim().isEmpty()) {
+            TextView body = text(message, 14, MUTED, false);
+            body.setLineSpacing(0, 1.08f);
+            panel.addView(body, margin(lp(-1, -2), 0, 0, 0, 16));
+        }
+
+        if (positiveLabel != null) {
+            Button positive = compactButton(positiveLabel);
+            positive.setBackground(roundRect(RED, 9, 0, 0));
+            positive.setTextColor(Color.BLACK);
+            positive.setOnClickListener(v -> {
+                dialog.dismiss();
+                if (positiveAction != null) positiveAction.run();
+            });
+            panel.addView(positive, lp(-1, dp(52)));
+        }
+        if (negativeLabel != null) {
+            Button negative = compactButton(negativeLabel);
+            negative.setTextColor(RED);
+            negative.setOnClickListener(v -> dialog.dismiss());
+            panel.addView(negative, margin(lp(-1, dp(48)), 0, 8, 0, 0));
+        }
+
+        dialog.setContentView(panel);
+        Window w = dialog.getWindow();
+        if (w != null) w.setBackgroundDrawableResource(android.R.color.transparent);
+        dialog.show();
+        if (w != null) w.setLayout((int)(getResources().getDisplayMetrics().widthPixels * 0.92f), ViewGroup.LayoutParams.WRAP_CONTENT);
+    }
+
     private void showTechnicalLogDialog() {
@@ -1342,10 +1392,7 @@
         Button clear = compactButton("AZZERA CRONOLOGIA TECNICA");
         clear.setTextColor(RED);
-        clear.setOnClickListener(v -> new AlertDialog.Builder(this)
-                .setTitle("Azzerare la cronologia tecnica?")
-                .setPositiveButton("AZZERA", (d, w) -> {
+        clear.setOnClickListener(v -> showAppConfirmDialog("AZZERARE LA CRONOLOGIA TECNICA?", "",
+                "AZZERA", () -> {
                     TechnicalLog.clear(this);
                     body.setText("Nessun ciclo tecnico registrato.");
-                })
-                .setNegativeButton("ANNULLA", null)
-                .show());
+                }, "ANNULLA"));
@@ -1439,14 +1486,11 @@
     private void cancelCurrentCycle() {
         if (!armed) return;
-        new AlertDialog.Builder(this)
-                .setTitle("Annullare il ciclo?")
-                .setMessage("L’uscita verrà spenta immediatamente e l’Inching verrà disattivato.")
-                .setPositiveButton("ANNULLA CICLO", (d, w) -> {
+        showAppConfirmDialog("ANNULLARE IL CICLO?",
+                "L’uscita verrà spenta immediatamente e l’Inching verrà disattivato.",
+                "ANNULLA CICLO", () -> {
                     setStatusPresentation("ANNULLAMENTO", "Spengo l’uscita e disattivo Inching…", RED);
                     cancelCycleButton.setEnabled(false);
                     Intent i = new Intent(this, SonoffArmService.class).setAction(SonoffArmService.ACTION_CANCEL);
                     startServiceCompat(i);
-                })
-                .setNegativeButton("CONTINUA", null)
-                .show();
+                }, "CONTINUA");
     }
@@ -1588,9 +1632,11 @@
     private Button modeButton(String s) {
         Button b = new Button(this);
         b.setText(s);
-        b.setTextSize(15);
+        b.setTextSize(13);
         b.setTypeface(Typeface.DEFAULT, Typeface.BOLD);
         b.setAllCaps(false);
-        b.setPadding(0, 0, 0, 0);
+        b.setGravity(Gravity.CENTER);
+        b.setLineSpacing(0, 0.94f);
+        b.setPadding(dp(2), dp(3), dp(2), dp(3));
         return b;
     }
